# Private Content Notice

`content/posts/` 用于存放本地 MDX 文章内容。

出于开源仓库的隐私考虑，真实文章文件默认通过根目录 `.gitignore` 忽略，不会被提交。

如果你需要在本地预览或同步文章：

- 直接把私有 `.mdx` 文件放到当前目录即可
- 不要移除 `.gitkeep` 和本说明文件
- 如需公开示例文章，请手动评估后再取消忽略并提交
